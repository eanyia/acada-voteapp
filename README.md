# naijavote
- This app uses a postgres DB and a Maven built application to simulate a voting application for Nigeria popular political parties.
- This app also requires the deployer to setup env values in an env file.
